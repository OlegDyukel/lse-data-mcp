name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

# Supersede in-flight runs for the same ref rather than paying for all of them.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      # Third-party actions are pinned to a commit, not a tag: a tag can be
      # repointed at new code by whoever owns the repository.
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package and development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Check formatting
        run: ruff format --check .

      - name: Lint
        run: ruff check .

      - name: Type check
        run: mypy src tests

      - name: Run unit tests
        run: pytest

      # The desktop bundle's manifest and dependency pin are generated from the
      # version in pyproject.toml. Checking them here turns a forgotten bump into
      # a pull-request failure, rather than one discovered at release time when
      # the tag has already been pushed and the number cannot be reused.
      - name: Check the desktop bundle is in step with the version
        run: python scripts/sync_bundle_version.py --check

  audit:
    name: Dependency audit
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Install package and audit tool
        run: |
          python -m pip install --upgrade pip
          python -m pip install . pip-audit

      # There is no lockfile, so each run resolves the dependency tree afresh.
      # Audit what actually got installed rather than what was requested.
      - name: Audit resolved dependencies
        run: pip-audit --progress-spinner off
